// app/(tabs)/scan.tsx
// Placeholder — the ScanTabButton in _layout intercepts the press; this screen never renders.
import { View } from 'react-native';
export default function ScanScreen() {
  return <View style={{ flex: 1, backgroundColor: '#000' }} />;
}
